#pragma once
#include "parsers.h"

namespace HedgeEdit::Data::Parsers
{
    DECLARE_PARSER(ResMirageMaterial);
    DECLARE_PARSER(ResModel);
    DECLARE_PARSER(ResMirageTerrainInstanceInfo);
    DECLARE_PARSER(ResMirageTerrainModel);
}
